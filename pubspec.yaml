name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # 🔹 Cập nhật các package mới nhất, tương thích Flutter 3.9+
  youtube_player_flutter: ^9.1.3
  flutter_tts: ^4.2.3
  provider: ^6.1.2
  flutter_inappwebview: ^6.1.5  # Thêm bản mới để sửa lỗi namespace
  cupertino_icons: ^1.0.8
  speech_to_text: ^7.3.0     # Nhận dạng giọng nói
  firebase_core: ^3.15.2
  firebase_auth: ^5.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/imgs/
